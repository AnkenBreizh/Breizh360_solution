Tests d’intégration API via WebApplicationFactory. Nécessite `public partial class Program { }` dans l’API.

TODO: quand on active les tests d’intégration, ajouter le package `Microsoft.AspNetCore.Mvc.Testing` (version alignée sur la runtime .NET 10) et ajouter `public partial class Program { }` dans l’app.
